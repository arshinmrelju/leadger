{
  "name": "seva-ledger",
  "private": true,
  "version": "0.4.0",
  "description": "SEVA LEDGER — digital service shop ledger (static Firebase app)",
  "type": "module",
  "scripts": {
    "test": "node --test tests/ledger.mjs"
  }
}