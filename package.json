{
  "name": "trustx-ledger",
  "private": true,
  "version": "0.10.0",
  "description": "TrustX Ledger — digital service shop ledger (static Firebase app)",
  "type": "module",
  "scripts": {
    "test": "node --test tests/ledger.mjs tests/module-graph.mjs"
  }
}